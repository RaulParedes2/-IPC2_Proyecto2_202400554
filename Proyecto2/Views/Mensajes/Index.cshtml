@using Proyecto2.TDAs
@model ListaMensajes

@{
    ViewData["Title"] = "Mensajes";
}

<h2>Gestión de Mensajes</h2>

<div class="mb-3">
    <a href="/Mensajes/Crear" class="btn btn-primary">Crear Nuevo Mensaje</a>
</div>

@if (TempData["Mensaje"] != null)
{
    <div class="alert alert-success">@TempData["Mensaje"]</div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

@if (Model.Count == 0)
{
    <div class="alert alert-info">
        No hay mensajes registrados. <a href="/Mensajes/Crear">Cree uno nuevo</a>
    </div>
}
else
{
    <div class="row">
        @{
            var actual = Model.GetPrimero();
            while (actual != null)
            {
                <div class="col-md-4 mb-3">
                    <div class="card h-100">
                        <div class="card-header bg-primary text-white">
                            <h5 class="mb-0">@actual.Data.Nombre</h5>
                        </div>
                        <div class="card-body">
                            <p><strong>Texto:</strong> @(string.IsNullOrEmpty(actual.Data.TextoOriginal) ? "Sin texto" : actual.Data.TextoOriginal)</p>
                            <p><strong>Instrucciones:</strong> @actual.Data.Instrucciones.Count</p>
                        </div>
                        <div class="card-footer">
                            <div class="btn-group w-100" role="group">
                                <a href="/Mensajes/Detalle?nombre=@actual.Data.Nombre" class="btn btn-info btn-sm">Detalle</a>
                                <a href="/Mensajes/GraficoPlan?nombre=@actual.Data.Nombre" class="btn btn-secondary btn-sm">Gráfico</a>
                                <form action="/Mensajes/Eliminar" method="post" style="display:inline">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="nombre" value="@actual.Data.Nombre" />
                                    <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('¿Eliminar este mensaje?')">Eliminar</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
                actual = actual.Siguiente;
            }
        }
    </div>
}

<div class="mt-3">
    <a href="/" class="btn btn-secondary">← Volver al Inicio</a>
</div>